# 文件管理系统实用优化方案

## 1. 最终目标

本项目的目标不是做一个功能很多的平台，而是做一个：

> **解压就能用、双击就能开、操作看得懂、出错能恢复的便携工具。**

所有优化都围绕以下三点：

1. **方便**：普通用户不需要安装 Node.js，不需要使用命令行，也不需要看开发文档。
2. **便携**：整个程序放在一个文件夹中，可以压缩、复制到其他 Windows 电脑上直接使用。
3. **易用**：只保留实际会使用的浏览、预览、撤回音频整理、撤回文档处理和日志功能。

本方案只列出能在当前 `Node.js + Express + 原生 HTML` 架构中直接落地的改动，不考虑大规模技术重构。

## 2. 明确不做的内容

为了避免项目越来越重，以下内容暂不实施：

- 不迁移 React、Vue、Electron 等新框架。
- 不增加登录、账号、权限管理和联网功能。
- 不引入数据库、复杂任务队列或后台管理系统。
- 不做通用规则编辑器，继续服务“返场故事撤回”这一固定场景。
- 不追求手机端使用，主要保证常见 Windows 桌面分辨率正常。
- 不增加普通用户不会使用的复制、重命名、在线协作等通用文件管理功能。
- 不为了代码形式好看而进行大规模拆分，只有影响维护和稳定性的部分才拆分。

## 3. 当前项目评估

### 已经做得比较好的部分

- 已有便携版构建脚本，可以打包 Node.js 运行环境和依赖。
- 页面已经具备目录树、文件预览、批量操作、进度反馈和操作日志。
- Word 支持原样视图、阅读视图、目录和搜索；Excel、文本、日志和图片也能预览。
- 删除 Word 内容前会备份原文件。
- 页面使用本地服务，文件不会上传到网络。
- 古籍风格和项目主题匹配，可以继续保留。

### 当前最影响使用的问题

#### 启动方面

- 开发目录中的 `start.bat` 会在缺少依赖时执行 `npm install`，换电脑后仍可能要求用户安装 Node.js 或联网。
- `启动文件管理系统.vbs` 实际会显示命令行窗口，与“静默启动”预期不一致。
- 端口固定为 `3000`，端口被其他程序占用时，系统会直接假设服务已经启动。
- 静默启动后没有方便的停止入口，用户可能只能去任务管理器结束 `node.exe`。
- 服务端和 VBS 都可能打开浏览器，存在重复打开页面的可能。

#### 界面方面

- 顶部按钮文字过长，三个批处理按钮、日志和说明同时出现，视觉压力较大。
- 路径输入框一直占据顶部空间，但普通用户通常只使用默认目录，不会频繁修改路径。
- “查找文档”和“删除文档内容”其实是一个连续流程，却被拆成两个独立按钮。
- 高风险删除操作虽然有确认框，但确认前看不到完整的待处理文件清单。
- 处理进度直接写在按钮中，文件名较长时按钮会变形，用户也不容易查看整体结果。
- 顶部格言和动效有主题特色，但占用了核心操作空间。

#### 功能方面

- 页面写的是“移动撤回音频”，后端实际会移动所有文件名中包含“撤回”的文件，可能误移动 Word、图片或其他文件。
- 删除操作会从匹配段落一直删除到 Word 文档结尾，风险较高。
- 虽然有备份文件，但页面没有“一键恢复”功能，普通用户不知道怎样恢复。
- 目录树会一次递归读取全部文件，大目录下启动较慢。
- 部分目录读取错误会被直接跳过，用户不知道哪些文件没有被扫描。

## 4. 推荐的最终使用方式

### 用户拿到程序后

1. 解压 `大明帝国文件管理.zip`。
2. 将程序文件夹放到需要管理的目录中。
3. 双击 `启动文件管理系统.vbs`。
4. 浏览器自动打开，直接显示上级目录中的文件。
5. 使用结束后双击 `停止文件管理系统.vbs`。

整个过程不需要安装 Node.js、不需要联网、不需要执行 `npm install`，也不显示命令行窗口。

### 页面内的核心操作

页面只突出两个实际工作入口：

1. **整理撤回音频**：扫描音频文件，展示结果，确认后移动到“撤回文件夹”。
2. **处理撤回文档**：扫描 Word 文档，展示匹配位置和文件清单，用户勾选后删除指定内容。

操作日志、目录设置和使用说明放到右上角的图标菜单中，不与核心任务抢占空间。

## 5. 项目启动便捷度优化

### 启动优化项

#### 1. 发布版真正免安装（必须）

发布包中固定包含：

```text
大明帝国文件管理/
  启动文件管理系统.vbs
  停止文件管理系统.vbs
  使用说明.txt
  runtime/node.exe
  node_modules/
  server.js
  index.html
  libs/
```

- 普通用户只需要看到“启动”“停止”和“使用说明”三个文件。
- `runtime` 只存放内置 Node.js，程序文件和依赖继续放在发布版根目录，保持当前服务端默认管理上级目录的逻辑不变。
- 如需减少误删，可以将内部文件夹设置为隐藏，但不要把 `server.js` 移到新的层级，否则必须同步修改默认根目录计算。
- 发布版启动时绝不执行 `npm install`。
- 打包者可以联网构建，但打包后的程序必须完全离线可用。

#### 2. 修正静默启动（必须）

- VBS 直接隐藏启动内置的 `node.exe`，不再调用会显示窗口的 `start.bat`。
- 只由启动脚本打开一次浏览器，服务端不再重复执行打开浏览器命令。
- 启动脚本等待服务可用后再打开页面，避免浏览器先出现“连接失败”。

#### 3. 自动处理端口冲突（建议）

- 优先使用 `3000`，被占用时依次尝试 `3001-3010`。
- 服务启动后把实际端口写入程序目录中的临时状态文件。
- 启动脚本根据状态文件打开正确地址。
- 如果检测到本项目已经运行，直接打开已有页面，不再启动第二个进程。

#### 4. 提供停止入口（必须）

- 启动成功后记录本项目的进程 ID。
- `停止文件管理系统.vbs` 先校验 PID 对应的进程路径、启动参数和内置 `runtime/node.exe` 是否匹配，确认是本项目后才结束进程，不能结束电脑上的其他 Node 程序。
- 程序异常退出后自动清理过期的进程和端口状态文件。

#### 5. 启动错误说人话（建议）

启动失败时生成 `启动错误.log`，并只提示用户能处理的信息：

- 程序文件缺失，请重新解压。
- 当前目录没有写入权限，请移动到其他目录。
- 端口全部被占用，请关闭旧程序后重试。
- 安全软件阻止运行，请允许本程序的内置 Node.js。

### 启动验收标准

- 在没有安装 Node.js 的 Windows 电脑上可以使用。
- 断网状态下可以正常启动和使用。
- 双击启动后只打开一个浏览器页面，不出现命令行窗口。
- 正常情况下 3-5 秒内进入系统。
- 双击停止入口后，本项目进程完全退出。

## 6. 界面 UI 优化

### 保留现有整体结构

继续使用“左侧目录树 + 右侧文件预览”的布局，不重新设计成复杂后台系统。

古籍米白色、朱砂红和宋体标题继续保留，但减少纯装饰区域，把更多空间留给文件和操作结果。

### 顶部区域调整

建议顶部只保留：

```text
大明帝国 · 文件管理 | 当前目录名称      刷新  设置  日志  帮助
```

- 当前路径默认只显示目录名，鼠标悬停时查看完整路径。
- 修改根目录放到“设置”弹窗中，避免一直显示长输入框。
- “刷新”“设置”“日志”“帮助”使用图标按钮并带提示文字。
- 格言可以缩小为标题旁的一行小字，取消粒子动画和全屏展示。

### 核心操作区调整

工具栏只保留两个大按钮：

- `整理撤回音频`
- `处理撤回文档`

不要再把“查找文档”和“删除内容”拆成两个按钮。“处理撤回文档”内部按照以下步骤完成：

```text
开始扫描 -> 显示匹配文件 -> 用户勾选 -> 确认删除 -> 显示结果
```

### 扫描结果弹窗

批量操作前必须显示：

- 扫描到的文件总数。
- 符合条件的文件数量。
- 每个文件的名称和相对路径。
- Word 文档中匹配的文字位置。
- 默认全选，并允许取消不想处理的文件。
- 最终按钮显示具体数量，例如“移动 12 个音频”或“处理 3 个文档”。

### 进度和结果

- 不再把长文件名写进按钮。
- 弹窗内显示统一进度条：`正在处理 6/20`。
- 当前文件名单独一行显示，过长时省略并可悬停查看。
- 完成后显示“成功、跳过、失败”数量。
- 修改 Word 后直接显示“恢复本次修改”按钮。

### 目录和预览区

- 左侧增加一个简单的文件名搜索框，只过滤当前已加载的目录树。
- 保留侧栏拖动宽度功能，并记住用户上次设置的宽度。
- 目录树改为点击文件夹时再读取子目录，避免启动时扫描全部文件。
- 预览标题显示文件名、类型、大小和修改时间。
- Word、Excel、文本和图片现有预览能力继续保留，不增加暂时用不到的编辑功能。

### UI 验收标准

- 1366×768 和 1920×1080 下按钮不重叠、不遮挡。
- 首次使用者能直接判断应该点击“整理撤回音频”还是“处理撤回文档”。
- 删除文档内容前一定能看到并选择待处理文件。
- 用户不需要阅读完整说明，也能完成核心流程。

## 7. 功能优化

### 必须修正的现有功能

#### 1. 只移动音频文件

“整理撤回音频”只处理以下扩展名：

```text
.mp3 .wav .m4a .aac .flac .ogg .wma
```

文件名还必须包含“撤回”。Word、Excel、图片、日志和其他文件不能被移动。

目标目录仍使用“撤回文件夹”，重名时保留现有的自动编号方式。

#### 2. 文档处理先扫描后删除

- 先扫描所有 `.docx` 文件。
- 展示同时包含“返场故事”和“撤回”的段落内容。
- 明确显示删除范围：从命中段落开始，删除该段及其后的全部内容，避免用户误解。
- 用户勾选需要处理的文件后才能执行。
- 执行时只允许处理本次扫描得到、且仍位于当前根目录中的文件。
- `.doc` 旧格式如果不能可靠修改，只提供查找和提示，不执行删除。

#### 3. 加强 Word 修改安全性

保留当前 ZIP/XML 处理方式，但增加以下保护：

1. 操作前创建带时间的备份目录。
2. 修改结果先写入临时文件，不直接覆盖原文件。
3. 使用 JSZip 和 Mammoth 再次读取临时文件，确认文档结构可打开。
4. 校验成功后再替换原文件。
5. 任一步失败都保留原文件，并在结果中显示失败原因。

#### 4. 增加一键恢复

- 每次文档处理记录原文件和备份文件的对应关系。
- 结果弹窗提供“恢复本次修改”。
- 日志页面允许恢复最近的历史操作。
- 恢复前再次确认，已有同名文件时先额外备份当前版本。

#### 5. 限制可操作路径

- 文件预览、移动、删除和恢复都只能操作当前根目录中的文件。
- 后端对所有路径使用 `path.resolve` 校验，拒绝 `..` 等越界路径。
- 服务只监听 `127.0.0.1`，避免局域网其他电脑调用接口。

### 建议做但可以排在后面的功能

- 大目录懒加载，提升首次打开速度。
- 文本、Word 和 Excel 增加文件大小限制，超大文件提示使用本地软件打开。
- 操作结果增加“打开目标文件夹”和“复制错误信息”。
- 日志增加按日期和操作类型筛选。
- 将“返场故事”“撤回”和音频扩展名放入简单的 `配置.json`，需要时由维护人员修改，不在页面中增加复杂规则编辑器。

### 功能验收标准

- 非音频文件不会被“整理撤回音频”移动。
- 文档处理前用户能看到并取消任意待处理文件。
- Word 修改失败时原文件不受影响。
- 每次 Word 修改都能通过页面恢复。
- 页面传入根目录外的路径时，服务端必须拒绝操作。

## 8. 实施顺序

### 第一阶段：先做到真正便携

1. 整理便携版目录结构。
2. 修正静默启动和重复打开浏览器的问题。
3. 增加进程记录和安全停止入口。
4. 在无 Node.js、无网络的电脑上测试发布包。

完成这一阶段后，程序应达到“解压、双击、能用”。

### 第二阶段：先看结果再操作

1. 修复只移动音频文件的问题。
2. 合并文档查找和删除流程。
3. 增加扫描结果选择、明确确认和统一进度弹窗。
4. 增加 Word 临时文件校验和一键恢复。

完成这一阶段后，程序应达到“操作简单、不会轻易误处理”。

### 第三阶段：界面收尾和速度优化

1. 精简顶部区域，只保留两个核心任务入口。
2. 将路径、日志和帮助移入图标菜单。
3. 增加目录搜索、侧栏宽度记忆和文件信息。
4. 将目录树改为按需加载。
5. 增加自动端口和启动状态提示，处理 3000 端口被占用的情况。

完成这一阶段后，程序应达到“界面清楚、打开更快、日常使用顺手”。

## 9. 最终优先级

| 优先级 | 优化项 | 是否必须 |
| --- | --- | --- |
| 1 | 免安装离线发布包 | 必须 |
| 2 | 真正静默启动、安全停止入口 | 必须 |
| 3 | 只移动音频文件 | 必须 |
| 4 | 文档先扫描选择、再确认删除 | 必须 |
| 5 | 修改校验和一键恢复 | 必须 |
| 6 | 路径边界和仅本机访问 | 必须 |
| 7 | 精简顶部按钮和核心流程 | 建议 |
| 8 | 自动端口和启动状态提示 | 建议 |
| 9 | 目录懒加载和大文件限制 | 建议 |
| 10 | 日志筛选、打开文件夹等辅助功能 | 可选 |

## 10. 最终发布验收

发布前必须在一台没有安装 Node.js、没有项目源码、断开网络的 Windows 电脑上验证：

- 解压后双击启动脚本可以打开系统，且只打开一个浏览器页面。
- 默认目录正确，Word、Excel、文本和图片可以预览。
- “整理撤回音频”不会移动非音频文件。
- “处理撤回文档”会先显示文件清单和删除范围，取消后不会修改文件。
- 执行 Word 修改后可以通过页面恢复，原文件和恢复文件都能正常打开。
- 双击停止脚本后，本项目的内置 Node.js 进程退出，其他 Node 程序不受影响。
- 重新启动后不会重复使用旧的 PID 或端口状态文件。

## 11. 最终结论

项目不需要变成通用文件管理平台，也不需要更换技术框架。最有价值的方案是：

> **发布包完全离线，用户只需双击启动；页面只突出两个实际任务；任何修改都先展示结果、允许选择，并且能够一键恢复。**

先完成这些改动，再根据实际使用中遇到的问题补功能。没有明确使用场景的功能暂时不做。
